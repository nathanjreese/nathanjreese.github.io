name: Indycar secrets
run-name: Indycar Driver App 🚀
on: [push]
jobs:
  default_env_variables:
    runs-on: ubuntu-latest
    steps:
    - name: testing-secrets
      uses: actions/checkout@v4
      env: 
        VITE_DATABASE_PASSWORD: ${{ secrets.PLANETSCALE_PW }}
        VITE_TEST_IN_ENV: ${{ secrets.TEST_VALUE }}
        VITE_TEST_NOT_ENV: ${{ secrets.TEST_VALUE2 }}